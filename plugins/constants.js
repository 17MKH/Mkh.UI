/**
 * 框架名称
 */
const UI_NAME = 'mkh-ui'

/**
 * 模块前缀
 */
const MODULE_PREFIX = '$mkh-mod-'

/**
 * 导入模块时的前缀
 */
const IMPORT_MODULE_PREFIX = '$mkh-mod-'

/**
 * 导入页面时的前缀
 */
const IMPORT_PAGE_PREFIX = '$page-'

/**
 * 导入皮肤时的前缀
 */
const IMPORT_SKIN_PREFIX = 'mkh-skin-'

/**
 * 导入语言包时的前缀
 */
const IMPORT_LOCALE_PREFIX = '$mkh-locale'

export { UI_NAME, MODULE_PREFIX, IMPORT_MODULE_PREFIX, IMPORT_PAGE_PREFIX, IMPORT_SKIN_PREFIX, IMPORT_LOCALE_PREFIX }

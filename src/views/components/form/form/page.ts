import type { PageDefinition } from '@/types'
import component from './index.vue'

const page: PageDefinition = {
  icon: 'form',
  name: 'doc_form',
  path: '/doc/form',
  component,
}

export default page

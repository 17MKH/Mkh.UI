import type { PageDefinition } from '@/types'
import component from './index.vue'

const page: PageDefinition = {
  icon: 'container',
  name: 'doc_layout_container',
  path: '/doc/layout/container',
  component,
  buttons: {},
}

export default page
